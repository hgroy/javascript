const displayMsg = " i am harsh gaurav from bihar ";
 // by using charat()
 const index = 9; 
//   console.log(displayMsg.charAt(index));

const char = displayMsg.charAt(index);
console.log(char);

//charcodeAt  gives ascii number of given characyer

  const ascii = displayMsg.charCodeAt(20);
  console.log(ascii);


  // 

