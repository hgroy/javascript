// trim means cut or delete something in the string 

const text = "               Harsh gAURAV           ";
console.log(text.length);


// we dont want spaces that why we have to cut it 

console.log(text.trim());
console.log(text);
console.log(text.trim().substring(0,6));